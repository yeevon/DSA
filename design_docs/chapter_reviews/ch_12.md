# Chapter 12 — As-Is Review

**Scope:** `chapters/ch_12/lectures.tex` (300 lines) + `chapters/ch_12/notes.tex` (139 lines). `practice.md` excluded per Phase-4 deferral.

**Purpose:** Step 1 of the per-chapter loop. Inventory of what's currently in ch_12 — topics, depth, callouts, mastery checklist (if any), cross-references — so Step 2 (CLRS + OCW gap analysis) has a baseline to compare against. **No augmentation suggestions in this doc.**

**Subtitle (from `\title`):** *Sets*

**Source coupling:** ch_12 is one of the **optional / post-SNHU** chapters (the SNHU-required arc covers ch_1–ch_6). The framing is **ADT-first survey register** — one abstract data type unfolded across operations, cost, static-vs-dynamic flavour, and container choice — closer to ch_10's breadth-survey register than to ch_9 / ch_11's depth-first single-algorithm register. C++ remains the implementation language but the focus is on STL idioms (`std::set`, `std::unordered_set`, `<algorithm>` set ops) rather than hand-rolled data structures. The 40-page `lectures.pdf` ceiling is **waived for optional chapters** under the post-2026-04-25 framing — ch_12 ships at 6 pp / 300 source lines, the shortest optional chapter inventoried so far (ch_7: ~30 pp / ch_9: 31 pp / 1575 lines / ch_10: 18 pp / 842 lines / ch_11: 9 pp / 432 lines). The register reads as "ADT-survey + container-picker" — heavy on cost-analysis tables and use-case-to-container mapping tables, light on hand-rolled algorithm bodies and figures.

---

## Chapter map

**Opening `ideabox` (lines 11–13)** — sits before §12.1 as an orienting "Set ADT in one sentence" frame. Verbatim:

> **The set ADT in one sentence.** A set is a collection of distinct elements with fast membership queries. It's the data structure you reach for any time you care about ``have I seen this before'' or ``how do these two collections compare,'' and almost nothing else. Everything in this chapter is about the operations that make sense on top of that single primitive.

**No formal "Where this sits / toolkit additions / mastery checklist" chapter map.** The opening `ideabox` is a one-paragraph thematic frame defining the ADT in one sentence and previewing that "everything else is built on this single primitive," not the structured chapter-map `ideabox` ch_1–ch_6 use. No "looking back / looking forward" enumerated bullets. No road-map subsection inside §12.1.

**No mastery checklist.** Absent (matches ch_7, ch_9, ch_10, ch_11). Step 2 will need to decide whether one belongs.

**Implicit chapter map (read off the section headings):**
- §12.1 The Set ADT: definition (unordered, distinct), three core ops (`add` / `remove` / `contains`), keys-vs-elements distinction, subset testing.
- §12.2 Set Operations: union / intersection / difference; C++ STL `<algorithm>` set ops on sorted ranges; hand-written hashed-set loops; "iterate the smaller set" warnbox; cost-analysis table; sorted-vs-hashed tradeoff; filter and map (FP-borrowed higher-order ops).
- §12.3 Static vs. Dynamic Sets: definitions; capability table; immutability rationale (thread safety / aggressive optimization / safe sharing / hashability); use-case-to-static-vs-dynamic table; C++ idioms for static sets (const wrap, `constexpr std::array`, gperf); immutability-is-a-spectrum notebox; "Sets you'll actually use" container table; chapter-takeaway closer.

**Forward refs / looking back:** §12.3 "Sets you'll actually use" tabular (line 285) cites "red-black tree (ch.\ 9)" as the typical implementation of `std::set` and `std::multiset` — the only explicit numbered cross-chapter pointer in the document. No forward refs to ch_13+. No dedicated end-of-chapter "looking forward / looking back" `ideabox` — the closing `ideabox` (line 296) is a chapter-takeaway summary, not a forward pointer.

---

## Section-by-section inventory (`lectures.tex`)

Legend for "Callouts": `D` = `defnbox`, `I` = `ideabox`, `W` = `warnbox`, `E` = `examplebox`, `N` = `notebox`. Counts are exact.

### Opening `ideabox` (pre-§12.1) — lines 11–13
- One-paragraph "set ADT in one sentence" frame defining a set as "distinct elements with fast membership queries" and previewing the chapter as "operations that make sense on top of that single primitive"; counted in §12.1's totals below for tally purposes (it sits between `\maketitle` and `\section{12.1 The Set ADT}`).
- **Callouts contributed:** I×1.

### 12.1 The Set ADT — lines 15–63
- **Subsections (`\subsection*`):** What a set is · Keys vs.\ elements · Subset testing
- **Callouts:** D×1 (l.19), I×1 (l.11 pre-§), W×0, E×0, N×1 (l.46)
- **Code listings (`lstlisting`):** 2 — (1) `std::unordered_set<int>`/`std::unordered_set<Student, StudentHash, StudentEq>` declaration showing element-is-key vs.\ explicit-hash-and-comparator forms, plus a `std::unordered_map<int, Student>` contrast for key/value separation (l.36); (2) `isSubset` body iterating the candidate-subset and probing the candidate-superset via `sup.count(x)` (l.54).
- **Tables (`tabular`):** 0
- **TikZ figures:** 0
- **Topics:** Formal "Set" `defnbox` (unordered collection of distinct elements, no duplicates, no position, $\{3,7,9\} = \{9,7,3\}$); itemized three-op API (`add(x)` / `remove(x)` / `contains(x)`) with the framing "everything else (union, intersection, filter, map, subset) is built on top of these three"; keys-vs-elements distinction (primitives are their own key; objects use one field as the key, "the rest is just cargo") with a `Student` example; C++ choice between element-is-key (`unordered_set<int>`) and explicit hash/equality functors (`unordered_set<Student, StudentHash, StudentEq>`), plus an `unordered_map<int, Student>` contrast; cross-language hash/equality contract `notebox` (Python `__hash__`/`__eq__`, Java `hashCode()`/`equals()`, "equal elements must hash equal — break that and the set is a corrupt data structure waiting to lose an element"); subset definition $X \subseteq Y$ iff every element of $X$ is in $Y$; trivial `isSubset` C++; cost: $O(|X|)$ for hashed superset, $O(|X| \log |Y|)$ for tree-based superset.
- **Cross-references:** None.
- **Depth markers:** Equal-elements-must-hash-equal contract is stated explicitly with cross-language scope (C++ / Python / Java) and the "corrupt data structure" failure-mode tag. Subset cost decomposition splits hashed vs.\ tree-based supersets without collapsing to "$O(|X|)$ amortized."

### 12.2 Set Operations — lines 65–205
- **Subsections:** Union, intersection, difference · C++ implementations · Cost analysis · Filter and map
- **Callouts:** D×2 (l.69, l.171), I×1 (l.163), W×1 (l.140), E×1 (l.77), N×1 (l.203)
- **Code listings:** 3 — (1) `std::set_union` / `std::set_intersection` / `std::set_difference` invocations on `std::set<int>` ranges via `std::inserter` (l.88); (2) hand-written `setUnion` / `setIntersect` / `setDiff` over `std::unordered_set<int>` showing the smaller-set / larger-set hoist for intersection (l.112); (3) templated `setFilter` / `setMap` over `std::unordered_set<T>` with predicate / function-object parameters and a lambda usage example (`evens = setFilter(nums, [](int x){...})`, `lengths = setMap<std::string, size_t>(words, ...)`) (l.177).
- **Tables:** 1 (l.149) — 4-row cost-analysis table: Union $O(|A|+|B|)$ "insert all" / Intersection $O(\min(|A|,|B|))$ "scan smaller, probe larger" / Difference $A \setminus B$ $O(|A|)$ "scan A, probe B" / Subset $A \subseteq B$ $O(|A|)$ "scan A, probe B".
- **TikZ figures:** 0
- **Topics:** Union / intersection / difference `defnbox` ($X \cup Y$ = either, $X \cap Y$ = both, $X \setminus Y$ = $X$-not-$Y$); commutativity statement (union/intersection commute, difference does not); 4-line worked `examplebox` evaluating $\{54,19,75\} \cup \{75,12\}$, $\cap$, $\setminus$, and the reverse-direction difference $\{75,12\} \setminus \{54,19,75\}$; STL `<algorithm>` set-ops claim — they operate on *sorted ranges*, "which means any sorted container"; full `std::set_union` / `std::set_intersection` / `std::set_difference` C++ block with `std::inserter` output iterators and inline expected-results comments; hand-rolled hashed-set `setUnion` (copy + range-insert) / `setIntersect` (smaller-vs-larger hoist + scan-smaller-probe-larger) / `setDiff` (scan-A-probe-B); "Always iterate the smaller set" `warnbox` framing the smaller-vs-larger hoist as "the single most common optimization in real set-heavy code"; cost-analysis tabular (assumes hashed sets with $O(1)$ expected ops); "for sorted (tree) sets, add a $\log n$ factor for each membership probe" trailing note; sorted-vs-hashed `ideabox` ("two-pointer merge on sorted sets matches hashed at $O(|A| + |B|)$ without per-element hashing; small + cache-fits → sorted can be faster; large + large-element-type → hashed wins; benchmark before picking"); filter / map `defnbox` (filter = predicate-restricted subset, map = $\{f(x) : x \in X\}$ "may be smaller than the input if $f$ is not injective"); templated `setFilter` / `setMap` C++ with usage examples (lambda-driven `evens` and `lengths`); cross-language framing ("Python / JavaScript / Rust / Haskell / Scala all have these as first-class methods on their set / collection types; C++ got closer in C++20 with `std::ranges::filter_view` / `std::ranges::transform_view`, but the syntax is still clunky"); filter-vs-map `notebox` ("Filter is a subset; map is not always" — $\{1, -1\} \to \{1\}$ via $f(x) = x^2$; "if you need one-to-one semantics, use a list or multiset and track duplicates explicitly").
- **Cross-references:** None numbered.
- **Depth markers:** `<algorithm>` set-ops sorted-range precondition is stated as a hard requirement, not assumed. Smaller-vs-larger intersection hoist is given its own warnbox separated from the listing. Sorted-vs-hashed tradeoff is articulated as "benchmark before picking" rather than collapsed to a default. Map non-injectivity collision is shown with a concrete worked input ($\{1, -1\} \to \{1\}$ under $x \mapsto x^2$). Cross-language filter/map availability is enumerated across five languages with a C++20 ranges concession.

### 12.3 Static vs.\ Dynamic Sets — lines 207–298
- **Subsections:** Why would you ever want a static set? · Choosing in practice · C++ idioms for static sets · Sets you'll actually use
- **Callouts:** D×1 (l.209), I×2 (l.232, l.296), W×0, E×0, N×1 (l.274)
- **Code listings:** 1 (l.263) — three C++ static-set idioms: `const std::unordered_set<std::string> STOP_WORDS{...}` (const-wrap); `constexpr std::array<int, 4> ALLOWED_CODES{...}` with a binary-search comment ("cache-friendly, no dynamic allocation"); a `// 3. Compile-time perfect hash via gperf, for pathological performance.` placeholder comment (no code body).
- **Tables:** 3 — (1) capability matrix (l.215, 7-row Operation-vs-Dynamic-vs-Static: Construct from collection / Count elements / Search-contains all yes/yes; **Add** dynamic-only; **Remove** dynamic-only; Union/intersection/difference returning new yes/yes; Filter/map returning new yes/yes); (2) use-case-to-static-vs-dynamic table (l.245, 6-row: allowed-country codes loaded at startup → static; stop-words for a search engine → static; users currently online → dynamic; session IDs issued today → dynamic; finalized vote tallies (after the election) → static; shopping cart contents → dynamic); (3) "Sets you'll actually use" 7-row container-vs-order-vs-impl table (l.281: `std::set` sorted / red-black tree (ch.\ 9); `std::unordered_set` hash order / open-addressed hash table; `std::multiset` sorted, allows dups / red-black tree; Python `set` hash order / open-addressed hash table; Python `frozenset` hash order / immutable hash table; Java `HashSet` / `TreeSet` -- / hash / red-black; Rust `HashSet` / `BTreeSet` -- / hash / B-tree).
- **TikZ figures:** 0
- **Topics:** Dynamic / static `defnbox` ("dynamic = modifiable after construction, supports add and remove; static = fixed at construction time, supports read-only and new-set-producing ops but not in-place add/remove"); capability tabular separating mutating ops (Add, Remove) from read / new-set ops; "Why would you ever want a static set?" `ideabox` enumerating four wins ("immutable buys you ...": thread safety for free / aggressive optimization (perfect hash, sorted array, compile-time constants) / safe sharing across consumers / hashability — "an immutable set can itself be a key in another set or map; Python's `frozenset` exists for exactly this"); 6-row "choosing in practice" table with concrete domain examples (allowed-country codes, stop-words, online-users, session IDs, finalized vote tallies, shopping-cart contents); C++ static-set workarounds (no built-in frozen set): const-wrap, `constexpr std::array` + `std::binary_search`, gperf compile-time perfect hash; immutability-spectrum `notebox` ("`const` in C++ is syntactic — someone can `const_cast` it away; Python's `frozenset` is structurally immutable; Rust and Haskell make the distinction first-class; if your codebase has the convention `const means truly immutable`, lean into static sets; otherwise you're one `const_cast` from heisenbugs"); "Sets you'll actually use" 7-row container-vs-order-vs-impl table covering C++ / Python / Java / Rust forms; chapter-takeaway `ideabox` (line 296) summarizing the chapter under "deduplicating, order-free, membership-query-oriented sibling of lists; iterate one side, probe the other; pick hash for $O(1)$ membership; pick tree for sorted iteration / `lower_bound`; pick static / frozen when no one should mutate; the ADT is simple, the engineering choices it unlocks are not."
- **Cross-references:** "Sets you'll actually use" tabular (line 285) cites "red-black tree (ch.\ 9)" for `std::set` and `std::multiset` — only explicit numbered cross-chapter pointer in the document.
- **Depth markers:** Static-set rationale enumerates four orthogonal benefits (thread safety, optimization, sharing, hashability) rather than collapsing to "you can't mutate it." Use-case table grounds the abstract distinction in six concrete domains. C++ idioms section acknowledges the absence of a built-in frozen set and the syntactic-only nature of `const`. Container survey spans four languages (C++, Python, Java, Rust) with order-vs-impl factored apart.

---

## Closing material (after §12.3)

**No closing summary section.** The final `ideabox` of §12.3 (line 296, labeled "Chapter takeaway") functions explicitly as a chapter-level closer — it states "the ADT is simple; the engineering choices it unlocks are not" and unifies the chapter under the three-way pick (hash for $O(1)$ membership / tree for sorted iteration or `lower_bound` / static-or-frozen for no-mutation). `\end{document}` follows on line 300. No "what this connects to / next chapter" forward-ref box. No companion-materials line. The closing matches ch_10 / ch_11's pattern of an explicitly labeled "Chapter takeaway" `ideabox` at the very end.

---

## `notes.tex` (compact reference, 139 lines, 2 pp)

**Layout:** Two-column `multicols`. Title: "Ch.~12 Notes — Sets". Single-page reference, no chapter map, no narrative — definitions, primitives, gotchas.

**Subsections (10):** The Set ADT · Keys vs.\ elements · Subset testing · Set operations · Always iterate the smaller set · Cost table · Static vs.\ dynamic · Containers you'll actually use · Picking the implementation · Common patterns · Top gotchas

**Callouts:** 0 of any class — notes.tex uses bare `\subsection*` headings and itemized lists / inline `\textbf` instead of the boxed-callout vocabulary. (lectures.tex carries all 13 boxes.)

**Tables:** 3 — (1) cost table (4-row, hashed vs.\ sorted-tree across contains / add-remove / union-intersection / sorted-iter / `lower_bound`, with `*` expected-time footnote); (2) static-vs-dynamic need-matching matrix (4-row: build-once-query / small-memory / mutate-at-runtime / tuned-lookup → static or dynamic with implementation hint); (3) containers-you'll-actually-use 6-row container-vs-order-vs-impl table (`std::set` / `std::multiset` / `std::unordered_set` / Python `set` / Python `frozenset` / Rust `HashSet`/`BTreeSet`).

**Code listings:** 1 — hashed iterate-and-probe intersection loop *plus* `std::set_intersection` STL call in the same listing block, side-by-side, showing the two idioms. No filter / map / setUnion / setDiff / subset / setFilter / setMap listings — those subsections are prose-only or table-only.

**Top gotchas (5 listed):**
1. Relying on iteration order in `unordered_set` — implementation-defined and libc-version-dependent.
2. Mutating elements in a sorted set in a way that changes their sort key — invalidates the invariant silently.
3. Using `set_intersection` / `set_union` from `<algorithm>` on *unsorted* ranges — requires sorted input.
4. Writing $A \setminus B$ as $A - B$ in math then forgetting which STL algorithm corresponds (`set_difference`).
5. Assuming "const" means truly immutable in C++ — someone can `const_cast` it away.

**Coverage relative to `lectures.tex`:**
- **Covered:** §12.1 ADT definition, three-op API (`contains` / `add` / `remove`), derived ops (`size` / `empty` / iteration); keys-vs-elements with comparator-vs-hash-and-equality framing; subset testing with iterate-smaller-probe-larger and the hashed vs.\ tree-based cost split; §12.2 union / intersection / difference symbols + commutativity; hashed iterate-and-probe + STL `set_intersection` listing; "always iterate the smaller set" with the $O(\min(|A|,|B|))$ payoff; cost table; §12.3 static vs.\ dynamic distinction; containers-you'll-actually-use table (`std::set` / `std::multiset` / `std::unordered_set` / Python `set` / Python `frozenset` / Rust `HashSet`/`BTreeSet`); picking-the-implementation rules with three concrete branches plus a Bloom-filter "approximate" branch; common patterns (dedup via `std::set<int>(v.begin(), v.end())` or `sort + unique + erase`; seen-set during BFS/DFS traversal; set-as-filter via whitelist / blacklist); 5 gotchas.
- **Not covered (intentionally — quick-reference card scope):** §12.1 keys-vs-elements `Student` worked example, the `unordered_map<int, Student>` key/value-separation contrast, the cross-language hash/equality contract `notebox` (Python `__hash__`/`__eq__`, Java `hashCode()`/`equals()`); §12.2 4-line worked `examplebox` evaluating union / intersection / difference on $\{54,19,75\}$ vs.\ $\{75,12\}$, the full hand-rolled `setUnion` / `setIntersect` / `setDiff` with smaller-vs-larger intersection hoist, the sorted-vs-hashed two-pointer-merge `ideabox` (the constant-overhead and cache-locality tradeoff), filter and map definitions / templated `setFilter` / `setMap` with the lambda-usage example, the cross-language filter/map enumeration (Python / JavaScript / Rust / Haskell / Scala) and the C++20-ranges concession, the filter-vs-map injectivity `notebox` ($\{1,-1\} \to \{1\}$ via $x^2$); §12.3 static-set capability tabular (7-row Operation × Dynamic × Static), four-bullet immutability-rationale `ideabox` (thread safety / optimization / sharing / hashability), 6-row use-case-to-static-or-dynamic table (allowed-country codes / stop-words / online-users / session IDs / vote tallies / shopping cart), C++ idioms listing (`const`-wrap, `constexpr std::array` + binary search, gperf placeholder), immutability-spectrum `notebox` (C++ `const` syntactic vs.\ Python `frozenset` structural vs.\ Rust/Haskell first-class), chapter-takeaway closer.
- **Net coverage:** notes.tex adds Bloom-filter as a fourth implementation branch (under "Picking the implementation") and enumerates a "Common patterns" subsection (dedup / seen-set / set-as-filter) that lectures.tex does *not* surface. lectures.tex carries the worked examples, the hand-rolled C++ bodies, the cross-language API enumerations, and the static-set rationale `ideabox`. Surfaced as inventory observation only.

**Density:** Quick-reference card — heavier on prose blocks (10 short subsections) than on code (1 listing) or tables (3 tabulars). Note that lectures.tex's `defnbox` for "Set" states "no duplicates, no position" but does not include a comparator/hash-and-equality contract spec; notes.tex's "Keys vs.\ elements" subsection spells the comparator (ordered) vs.\ hash-and-equality (unordered) split inline. Note also that notes.tex's "Common patterns" subsection (dedup / seen-set / set-as-filter) and the Bloom-filter "approximate" branch under "Picking the implementation" do not appear in lectures.tex. Surfaced as inventory observations for Step 2.

---

## Cross-references inside ch_12

- §12.1 → §12.2 (the three core ops `add`/`remove`/`contains` are the foundation §12.2's union/intersection/difference are built on; framed at §12.1's "everything else is built on top of these three" close).
- §12.1 → §12.2 (subset cost split into hashed $O(|X|)$ vs.\ tree-based $O(|X| \log |Y|)$ rhymes with §12.2's cost-analysis tabular's "for sorted (tree) sets, add a $\log n$ factor" trailing note).
- §12.2 → §12.2 (the "iterate the smaller set" warnbox is justified by the cost-analysis tabular row Intersection $O(\min(|A|,|B|))$ "scan smaller, probe larger").
- §12.2 → §12.3 (filter and map's "return new" framing — both are pure functions over the input set — is consistent with §12.3's capability-matrix row showing filter/map are the *only* mutating-looking operations both static and dynamic sets support, because they construct new sets).
- §12.3 → §12.3 (the immutability-rationale `ideabox` motivates the C++ static-set idioms; the immutability-spectrum `notebox` qualifies them with the "const is syntactic in C++" caveat).

## Forward- / backward-references to other chapters

- **§12.3 "Sets you'll actually use" tabular (line 285):** Cites "red-black tree (ch.\ 9)" as the typical implementation of `std::set` and `std::multiset` — only explicit numbered cross-chapter pointer in the document.
- **No forward refs to ch_13+.** ch_12 closes with the chapter-takeaway `ideabox` (line 296) without a "next, maps / dictionaries / ..." forward pointer.
- **No backward refs to chapters other than ch_9.** No reference to ch_10 (hash tables) despite §12.3's "open-addressed hash table" rows in the container tabular and §12.2's hashed-set assumption ("$O(1)$ expected insert and lookup"); no reference to ch_11 (B-trees) despite the Rust `BTreeSet` row; no reference to ch_5/ch_6 binary-tree material despite the red-black-tree pointer at ch.~9. The hashing and B-tree framings are invoked generically without section pointers.
- **No multiset-specific cross-reference.** `std::multiset` is named in the §12.3 "Sets you'll actually use" tabular ("sorted, allows dups; red-black tree") and in notes.tex's containers table. The chapter does not have a dedicated multiset section, definition, or cost analysis — surfaced as inventory observation.

---

## Stylistic patterns observed

1. **ADT-first survey register, not algorithm-depth register.** Unlike ch_11 (5 sections walking one structure end-to-end through definition / search / insert / remove / refinement), ch_12 takes 3 sections to unfold one ADT's *operations* and *flavour-choices* (mutability, sortedness). Each section has a clear narrative arc (definition → operations / cost → engineering tradeoff).
2. **Heavy `defnbox` density relative to other optional chapters.** 4 defnboxes (Set; union/intersection/difference; filter and map; static-vs-dynamic) — ch_11 had 3 defnboxes, ch_10 used a similarly defnbox-heavy pattern. Each `defnbox` introduces a vocabulary item the rest of the section depends on.
3. **Balanced `ideabox` density.** 4 ideaboxes (opening one-sentence-ADT / sorted-vs-hashed-revisited / why-static-set-buys-you-things / chapter-takeaway). Compare ch_10 (15 ideaboxes / 37 callouts ~41%), ch_11 (10 / 17 ~59%); ch_12 sits at 4/13 (~31%) — least ideabox-skewed of the optional chapters inventoried.
4. **Zero TikZ figures.** No structural diagrams (matches ch_10; contrasts ch_9's 29 / ch_7's 8 / ch_11's 1). The chapter is prose + tables + listings.
5. **Heavy `tabular` use for cost / capability / use-case / container surveys.** 4 tabulars across 3 sections — cost-analysis (§12.2), capability matrix dynamic-vs-static (§12.3), use-case-to-static-or-dynamic mapping (§12.3), "Sets you'll actually use" container-vs-order-vs-impl (§12.3). All small (4–7 rows). Container-survey table appears in *both* lectures.tex (7 rows) and notes.tex (6 rows) with overlapping but non-identical row sets.
6. **STL-idiom register, not hand-rolled-data-structure register.** Listings center on `std::set` / `std::unordered_set` / `<algorithm>` set ops + `std::inserter`, with hand-rolled bodies appearing only as templated wrappers over `std::unordered_set` (`setUnion` / `setIntersect` / `setDiff` / `setFilter` / `setMap`). No node-skeleton struct, no `template<typename T>` data-structure form (compare ch_9's `AVLNode<T>` and ch_11's `BTreeNode<Order>`). Smallest-vs-larger hoist is the only non-trivial algorithmic move shown.
7. **Cross-language API enumeration is consistent and thick.** Python `set`/`frozenset`, Java `HashSet`/`TreeSet`, Rust `HashSet`/`BTreeSet`, plus Haskell / Scala / JavaScript appearances under filter/map. ch_11 stayed within C++ (with one CLRS/Knuth taxonomy aside); ch_12 spans five mainstream languages. The cross-language framing carries weight comparable to the C++ STL idioms.
8. **Production / domain examples grounded in engineering scenarios, not industry citations.** "Allowed-country codes loaded at startup," "stop-words for a search engine," "users currently online," "session IDs issued today," "finalized vote tallies," "shopping cart contents." No named systems (compare ch_11's MySQL InnoDB / PostgreSQL / git packfile / ext4 / NTFS / APFS / Btrfs / BoltDB / LMDB enumeration).
9. **Sorted-vs-hashed presented as a benchmark-it tradeoff, not a default pick.** §12.2's `ideabox` ("on small sets that fit in cache, the sorted version can actually be faster, because hashing has nontrivial constant overhead") refuses the "hashed = fast / tree = slow" reduction. Pattern matches ch_11's 2-3-4-vs-large-K and Knuth-classical-vs-preemptive tradeoffs.
10. **No multiset depth.** `std::multiset` is named in two tables but never gets its own definition, cost analysis, or worked example. The chapter is set-not-multiset.
11. **No bitset / Bloom-filter / perfect-hash depth in lectures.tex.** Lectures mentions perfect hash (in §12.3's `ideabox` "Aggressive optimization" bullet) and gperf (in §12.3's C++ idioms listing as a `// 3.` placeholder comment with no body). Notes.tex adds bitset and Bloom filter to its "Picking the implementation" branch list. None of these get expanded into a definition / cost / use-case decomposition — surfaced as inventory observation.
12. **No chapter-opening structured map.** Pre-§12.1 `ideabox` is a one-paragraph thematic frame ("set ADT in one sentence") rather than the structured "Where this sits / toolkit additions / mastery checklist" `ideabox` of ch_1–ch_6. Matches ch_7 / ch_9 / ch_10 / ch_11.
13. **No mastery checklist.** Absent (matches ch_7 / ch_9 / ch_10 / ch_11). Step 2 will need to decide whether one belongs.
14. **Explicit chapter-takeaway closer** (line 296). Same labeling convention as ch_10 / ch_11's closers. ch_12's takeaway unifies the chapter under three engineering picks (hash / tree / static-or-frozen).
15. **Notes.tex extends lectures.tex coverage in two places.** Notes adds (a) a "Common patterns" subsection (dedup, seen-set, set-as-filter) and (b) a Bloom-filter branch under "Picking the implementation" — neither appears in lectures.tex. Surfaced as inventory observation only.

---

## Counts at a glance

| Metric | Count |
|---|---|
| Sections (numbered 12.1–12.3) | 3 |
| Subsections (`\subsection*`, unnumbered) | 11 |
| `defnbox` (definitions) | 4 |
| `ideabox` (key ideas) | 4 |
| `warnbox` (gotchas) | 1 |
| `examplebox` (worked examples) | 1 |
| `notebox` (asides) | 3 |
| **Total callout boxes** | **13** |
| `lstlisting` (code blocks) | 6 |
| `tabular` (tables) | 4 |
| `tikzpicture` (figures) | 0 |
| Mastery checklist items | 0 (no checklist) |
| Lines (`lectures.tex`) | 300 |
| Pages (`lectures.pdf`) | 6 |
| Lines (`notes.tex` compact) | 139 |
| Pages (`notes.pdf`) | 2 |
