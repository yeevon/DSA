# Chapter 10 — As-Is Review

**Scope:** `chapters/ch_10/lectures.tex` (842 lines) + `chapters/ch_10/notes.tex` (155 lines). `practice.md` excluded per Phase-4 deferral.

**Purpose:** Step 1 of the per-chapter loop. Inventory of what's currently in ch_10 — topics, depth, callouts, mastery checklist (if any), cross-references — so Step 2 (CLRS + OCW gap analysis) has a baseline to compare against. **No augmentation suggestions in this doc.**

**Subtitle (from `\title`):** *Graphs*

**Source coupling:** ch_10 is one of the **optional / post-SNHU** chapters (the SNHU-required arc covers ch_1–ch_6). The framing is **breadth-of-the-graph-toolkit exposition** — vocabulary, two representations, two traversals, three shortest-path algorithms, topological sort, MST, all-pairs — rather than the depth-first single-topic register ch_7 / ch_9 use. C++ is the implementation language but is no longer the subject. The 40-page `lectures.pdf` ceiling is **waived for optional chapters** under the post-2026-04-25 framing — ch_10 ships at 18 pp / 842 source lines, well under the SNHU-arc ceiling and noticeably shorter than ch_9 (31 pp / 1575 lines). The register reads as "broad survey with idiomatic C++ snippets and one or two-paragraph rationale per algorithm" — closer to the textbook-companion register of ch_1–ch_6 than to ch_9's CLRS-style derivation depth.

---

## Chapter map

**Opening `ideabox` (lines 11–13)** — sits before §10.1 as an orienting "Why graphs matter" frame. Verbatim:

> **Why graphs matter.** Every previous data structure in this course is really a specialization of a graph: a linked list is a path graph, a tree is a connected acyclic graph, a heap is a tree with an ordering, a hash-chained bucket is a forest. Graphs are the most general model of "structured connections," so they're where interesting algorithms live: shortest paths, scheduling, routing, dependency resolution, network flow, recommendation, social analysis. You'll use them more than any other data structure in the wild.

**No formal "Where this sits / toolkit additions / mastery checklist" chapter map.** The opening `ideabox` is a one-paragraph thematic frame, not the structured chapter-map `ideabox` ch_1–ch_6 use. No "looking back / looking forward" enumerated bullets. No road-map subsection inside §10.1.

**No mastery checklist.** Absent (matches ch_7, ch_9). Step 2 will need to decide whether one belongs.

**Implicit chapter map (read off the section headings):**
- §10.1 Introduction: vocabulary (graph / adjacent / path / distance / $|V|$ / $|E|$); dense vs.\ sparse and why it dominates algorithm choice.
- §10.2 Applications: navigation, recommendations, social networks, plus seven other domain examples.
- §10.3 Adjacency Lists: three C++ idioms; cost analysis; default for sparse.
- §10.4 Adjacency Matrices: matrix form; $V^2$ blow-up; matrix-power walk count; when-to-pick-which table.
- §10.5 Breadth-First Search: queue-based traversal; distance/parent tracking; FIFO-preserves-shortest-path invariant; six application bullets.
- §10.6 Depth-First Search: iterative + recursive forms; recursion-depth gotcha; BFS-vs-DFS table; "DFS produces ordering, BFS produces layering" framing.
- §10.7 Directed Graphs: digraph / cycle / DAG; in/out-degree / source / sink; kidney-exchange application.
- §10.8 Weighted Graphs: path-length-as-sum; Dijkstra-can't-handle-negative warnbox; representations.
- §10.9 Dijkstra's Shortest Path: priority-queue BFS; idiomatic C++ with stale-entry trick; greedy-correctness `ideabox`; runtime table by PQ choice; path reconstruction.
- §10.10 Bellman-Ford Shortest Path: relax-every-edge-$V-1$-times; negative-cycle detection; Dijkstra-vs-Bellman-Ford decision table; SPFA notebox.
- §10.11 Topological Sort: Kahn's (in-degree peel) + DFS post-order; cycle detection as a side effect.
- §10.12 Minimum Spanning Tree: Kruskal with union-find (path compression + union-by-rank); cut property; Prim sketch + Kruskal-vs-Prim table.
- §10.13 All-Pairs Shortest Path (Floyd-Warshall): three-loop $O(V^3)$; $k$-loop-outermost invariant; Floyd-Warshall-vs-$V$-Dijkstras table; negative-cycle-via-diagonal trick; path reconstruction; tropical-algebra "algebraic view"; chapter-closing `ideabox`.

**Forward refs / looking back:** Opening `ideabox` (line 12) cites linked list / tree / heap / hash chain as graph specializations (implicit refs to ch_2, ch_5–ch_6, ch_7, ch_8 — though ch_8 doesn't exist in cs-300's chapter map; this is a *generic* reference to hash-chained buckets, not a section pointer); §10.8 forward-refs §10.9 (Dijkstra), §10.10 (Bellman-Ford); §10.10 forward-refs §10.13 (Floyd-Warshall). No backward refs to specific prior chapter sections by number — graphs are introduced fresh, not as a refinement of an earlier ch_N construct. No dedicated end-of-chapter "looking forward / looking back" `ideabox` (see Closing material below).

---

## Section-by-section inventory (`lectures.tex`)

Legend for "Callouts": `D` = `defnbox`, `I` = `ideabox`, `W` = `warnbox`, `E` = `examplebox`, `N` = `notebox`. Counts are exact.

### Opening `ideabox` (pre-§10.1) — lines 11–13
- One-paragraph "Why graphs matter" frame; counted in §10.1's totals below for tally purposes (it sits between `\maketitle` and `\section{10.1 Introduction}`).
- **Callouts contributed:** I×1.

### 10.1 Introduction — lines 15–48
- **Subsections (`\subsection*`):** The vocabulary · The shape of the space
- **Callouts:** D×1, I×1 (the pre-§10.1 opening `ideabox`), W×0, E×1, N×1
- **Code listings (`lstlisting`):** 0
- **Tables (`tabular`):** 0
- **TikZ figures:** 0
- **Topics:** Formal $G = (V, E)$ definition with vertices, edges, adjacency, path, length, distance, $|V|$ / $|E|$ notation; "length is overloaded — unweighted = edge count, weighted = sum of weights" `notebox`; max-edge upper bound $\binom{V}{2} = \Theta(V^2)$ undirected, $V(V-1) = \Theta(V^2)$ directed; dense vs.\ sparse formal distinction (dense $|E| = \Theta(V^2)$, sparse $|E| = O(V)$ or $O(V \log V)$); "first question to ask about any graph algorithm: what does this cost on a sparse input?"; concrete-numbers `examplebox` (Facebook $V \approx 3 \times 10^9$, $E \approx 10^{12}$, $V^2 \approx 9 \times 10^{18}$ — adjacency matrix physically impossible).
- **Cross-references:** None to specific prior sections.
- **Depth markers:** Quantitative real-world example (Facebook order-of-magnitude calc); explicit "sparse-first design discipline" framing.

### 10.2 Applications — lines 50–86
- **Subsections:** Navigation (geography) · Product recommendations (e-commerce) · Social and professional networks · Other common models you'll encounter
- **Callouts:** D×0, I×1, W×0, E×0, N×1
- **Code listings:** 0
- **Tables:** 0
- **TikZ figures:** 0
- **Topics:** "Graphs model anything with binary relationships — recognizing the graph in a problem is 90% of the solution" `ideabox`; navigation (cities = vertices, roads = edges, weights = distance/time, real-time-traffic-makes-edges-dynamic note); product recommendations (products = vertices, "bought together" / "viewed together" = edges, basket → one-step walk); social/professional networks (people = vertices, relationships = edges, friend-of-friend = two-step walk; PageRank, centrality, community detection); seven-bullet "other common models" enumeration: compiler dependencies (topo sort of include DAG), spreadsheet formula deps (topo + cycle detection), package managers (constraint problem), build systems `make`/`bazel`/`cmake` (reachability), state machines (model checking = labeled reachability), game AI / pathfinding (A* — *named without a section pointer*), neural networks ("topology is literally a weighted DAG"); closing `notebox` framing graph-recognition as the abstraction's payoff.
- **Cross-references:** None.
- **Depth markers:** Breadth-of-application list spans seven domains (compilers, spreadsheets, package managers, build systems, state machines, game AI, neural nets). A* mentioned as "industry standard" but no algorithm body (see §10.9 — Dijkstra is covered, A* is not).

### 10.3 Adjacency Lists — lines 88–129
- **Subsections:** C++ representations · Cost analysis
- **Callouts:** D×1, I×1, W×0, E×0, N×0
- **Code listings:** 1 — three idioms in one block: (1) `std::vector<std::vector<int>>` integer-IDs form ("the fastest and most common choice in competitive programming"); (2) `std::vector<std::vector<std::pair<int,int>>>` weighted form; (3) `struct Vertex { std::string label; std::vector<Vertex*> neighbors; }` OOP form.
- **Tables:** 0
- **TikZ figures:** 0
- **Topics:** Adjacency-list formal definition; three C++ idioms with selection rationale; cost analysis bullets (space $O(V+E)$, "is $u$ adj to $v$?" $O(\deg u)$, list-neighbors $O(\deg u)$, add-edge $O(1)$, remove $O(\deg u)$); closing `ideabox` ("default choice for sparse graphs; NetworkX, boost::graph, JDK graph APIs all use lists by default").
- **Cross-references:** None.
- **Depth markers:** Production-library citations (NetworkX, boost::graph, JDK).

### 10.4 Adjacency Matrices — lines 131–180
- **Subsections:** Cost analysis · When to pick which
- **Callouts:** D×1, I×0, W×1, E×0, N×1
- **Code listings:** 1 — unweighted `std::vector<std::vector<int>>` initialized to 0 with $M[u][v] = 1$, plus weighted form using `INT_MAX` sentinel and $w[i][i] = 0$.
- **Tables:** 1 — 5-row list-vs-matrix comparison (space, edge query, list neighbors, iterate all edges, "good for...").
- **TikZ figures:** 0
- **Topics:** Adjacency-matrix formal definition with `INT_MAX` no-edge sentinel convention; $\Theta(V^2)$ space; $O(1)$ edge query; $O(V)$ list-neighbors; matrix-power walk-count claim ($M^k[i][j]$ = number of length-$k$ walks, "basis of matrix-multiplication shortest-path algorithms and a ton of algebraic graph theory" — connects forward to §10.13 algebraic-view subsection); list-vs-matrix tabular; concrete blow-up warnbox ($V = 10^5$ → 40 GB; $V = 10^6$ → 4 TB); edge-list-as-third-representation `notebox` ("perfect for Kruskal's MST, useless for most queries, perfect for streaming graphs through a network").
- **Cross-references:** Edge-list `notebox` forward-refs Kruskal (covered in §10.12 but not section-numbered in the prose).
- **Depth markers:** Quantitative space-blow-up warning with concrete byte counts. Matrix-power-as-walk-count claim is real algebraic-graph-theory depth (also surfaced again in §10.13 algebraic view).

### 10.5 Breadth-First Search — lines 182–262
- **Subsections:** Algorithm · Distance and parent tracking · Cost · Why BFS works -- the invariant · Typical applications
- **Callouts:** D×0, I×2, W×1, E×0, N×0
- **Code listings:** 2 — (1) basic BFS with `std::queue<int>` and `discovered[]` boolean; (2) augmented BFS computing `dist[]` and `parent[]` in addition.
- **Tables:** 0
- **TikZ figures:** 0
- **Topics:** "BFS in one sentence" opening `ideabox` (queue *is* the algorithm; FIFO ordering produces the distance-layered traversal); basic BFS code; mark-on-enqueue-not-on-dequeue invariant warnbox (mark on pop blows up the queue); distance + parent augmentation rationale ("BFS computes shortest paths in unweighted graphs for free"); augmented BFS code with `dist[v] == -1` as the "not yet discovered" sentinel; "follow `parent` pointers back from `v` to reconstruct path in reverse" prose; cost $O(V+E)$ ("can't be beat for unweighted shortest paths"); FIFO-preserves-shortest-path invariant `ideabox` (queue contains $\{d, d, \ldots, d, d+1, d+1, \ldots\}$ stretches; alternative paths would have been seen and shorter on an earlier iteration); typical-applications bullet list (six items: shortest path unweighted, friend-of-a-friend, connected components, bipartite check via 2-coloring, web crawling / scraping, closest-of-many search).
- **Cross-references:** None to specific prior sections.
- **Depth markers:** FIFO-invariant proof sketched symbolically; bipartite-check application is non-trivial; mark-on-enqueue invariant explicitly named.

### 10.6 Depth-First Search — lines 264–334
- **Subsections:** Iterative DFS · Recursive DFS · Cost · BFS vs.\ DFS: when to pick which
- **Callouts:** D×0, I×2, W×2, E×0, N×0
- **Code listings:** 2 — (1) iterative DFS with `std::stack<int>`, mark-on-pop-with-filter idiom; (2) recursive DFS body.
- **Tables:** 1 — 6-row BFS-vs-DFS comparison (auxiliary structure / shortest path / memory worst-case / explores / natural for / solves).
- **TikZ figures:** 0
- **Topics:** "DFS in one sentence" opening `ideabox` (stack *is* the algorithm); iterative DFS code; iterative-DFS-marks-on-pop-not-on-push warnbox ("the opposite of BFS"; mark-on-push reverses neighbor ordering subtly; idiomatic form filters at top of loop and tolerates duplicate stack entries); recursive DFS shorter form; recursion-depth warnbox (call stack as deep as $V$ on path-shaped graph; at $V \approx 10^5$ overflow on most platforms; "single most common bug in graduate-level graph assignments"); cost $O(V+E)$; BFS-vs-DFS tabular comparison; closing `ideabox` ("DFS produces ordering [finish times], BFS produces layering [distance from start]; topo sort + SCC from DFS finish times; Dijkstra + shortest paths from BFS-style frontier expansion").
- **Cross-references:** None to specific prior sections; closing `ideabox` previews topo sort, SCC, Dijkstra (all covered later in this chapter).
- **Depth markers:** Iterative-DFS subtle-ordering warning is implementation-detail depth. Recursion-depth gotcha is practitioner content. Ordering-vs-layering framing is the chapter's pivotal mental model.

### 10.7 Directed Graphs — lines 336–382
- **Subsections:** Why DAGs matter · Representation · Terminology that bites
- **Callouts:** D×1, I×0, W×0, E×1, N×0
- **Code listings:** 1 — one-line directed adjacency: `adj[u].push_back(v);` (no symmetric `v→u` push).
- **Tables:** 0
- **TikZ figures:** 0
- **Topics:** Directed-graph + cycle + DAG formal definitions, with "DAG = the single most useful graph class in computer science" callout claim; six-bullet DAG application list (course prereqs, build systems, spreadsheets, git history, neural-net forward passes, dataflow programs); cycle-as-contradiction framing ("A needs B, B needs A"); cycle detection on directed graphs = "DFS plus a 'currently in recursion' flag"; representation note (directed = only one push per edge); in-degree / out-degree / successor / predecessor / source / sink terminology bullets; kidney-exchange `examplebox` (directed edge $D \to R$ = "donor family of patient $D$ willing to give kidney to recipient $R$"; $k$-way simultaneous exchange = cycle of length $k$; "real-world optimization problem worth billions of dollars in life-years").
- **Cross-references:** None to specific prior sections; cycle-detection-via-DFS-recursion-flag is mentioned but not coded.
- **Depth markers:** Kidney-exchange application is striking and concrete. In-degree / out-degree / source / sink vocab is comprehensive. Cycle detection algorithm is hinted but not implemented (deferred? — this is inventory only, not a finding).

### 10.8 Weighted Graphs — lines 384–419
- **Subsections:** Why weights complicate things · Representation · Negative weights
- **Callouts:** D×1, I×0, W×1, E×0, N×0
- **Code listings:** 1 — list form `std::vector<std::vector<std::pair<int,int>>>` plus matrix form with `INT_MAX` no-edge and $w[i][i] = 0$ self-loop.
- **Tables:** 0
- **TikZ figures:** 0
- **Topics:** Weighted-graph formal definition (path length = sum of edge weights); weighted-vs-unweighted-shortest distinction with concrete A→C-direct-10 vs.\ A→B→C-via-2+3 example showing more-edges-can-be-shorter; "BFS can't see this — enter Dijkstra (§10.9)" forward ref; representation snippet; negative-edges-allowed-but-negative-cycles-break-shortest-path formal note (loop forever, total → $-\infty$, shortest path undefined); Dijkstra-can't-handle-negatives warnbox citing greedy-correctness-relies-on-no-revisit (forward ref to §10.10 Bellman-Ford and a special "Johnson's algorithm" mention without a body); "knowing which shortest-path algorithm tolerates which edge signs is a classic exam question."
- **Cross-references:** §10.9 (Dijkstra), §10.10 (Bellman-Ford), Johnson's algorithm (named, no body).
- **Depth markers:** Concrete A-B-C 10-vs-5 weighted-shortest-path counterexample. Negative-cycle-undefined formal explanation. Johnson's algorithm name-dropped (no implementation).

### 10.9 Dijkstra's Shortest Path — lines 421–511
- **Subsections:** Algorithm · Why it works -- greedy correctness · Cost · Reconstructing the path
- **Callouts:** D×0, I×2, W×1, E×0, N×0
- **Code listings:** 2 — (1) full templated Dijkstra with `std::priority_queue<PQE, std::vector<PQE>, std::greater<PQE>>` min-heap, `dist[]` + `prev[]`, stale-entry-skip idiom; (2) `pathTo` reconstruction walking `prev` backwards then reversing.
- **Tables:** 1 — 3-row PQ-implementation-vs-runtime (unsorted array $O(V^2 + E)$, binary heap $O((V+E) \log V)$, Fibonacci heap $O(E + V \log V)$).
- **TikZ figures:** 0
- **Topics:** "Dijkstra in one sentence" opening `ideabox` (BFS with priority-queue frontier keyed by distance; closest-unvisited expanded next; its distance is now final); per-vertex `dist[v]` (best-known shortest distance, init $\infty$) + `prev[v]` (previous vertex along best-known path, init null) state; relaxation rule (`if dist[u] + w < dist[v]` then update `dist[v]` and `prev[v]`); full templated Dijkstra body with min-heap pattern; stale-entry warnbox (`std::priority_queue` doesn't support decrease-key, so push-new-smaller-entry + check `if (d > dist[u]) continue;` on pop — "idiomatic C++ Dijkstra, simple, fast, what you'd write in interviews or contests"); greedy-correctness `ideabox` ("when we pop closest-remaining vertex $u$ with tentative distance $d$, no later path can be shorter — any other path goes through unvisited $v$ with $\texttt{dist}[v] \ge d$, then adds non-negative edge weight; this is exactly why Dijkstra requires non-negative edges"); PQ-implementation cost table (Fibonacci-heap cited as theoretical-best but "rarely wins in practice because of hideous constant factors"); path-reconstruction code; "if `prev[target] == -1` and `target != src`, the target is unreachable" prose.
- **Cross-references:** §10.10 (Bellman-Ford forward ref via the negative-edge greedy-correctness argument).
- **Depth markers:** Greedy-correctness proof argued symbolically. Fibonacci-heap-constant-factor practitioner note. Stale-entry idiom is real production-implementation depth (the std::priority_queue lacks decrease-key, so the workaround is pragmatic, not textbook). "Interview / contest" register signals competitive-programming framing.

### 10.10 Bellman-Ford Shortest Path — lines 513–585
- **Subsections:** Algorithm · Why $V-1$ iterations? · Cost · Dijkstra vs.\ Bellman-Ford, decision flowchart
- **Callouts:** D×0, I×1, W×0, E×0, N×1
- **Code listings:** 1 — `bellmanFord` returning `bool` (false on negative cycle), `struct Edge { int u, v, w; }` representation, main $V-1$-iteration relaxation loop, plus separate negative-cycle-check pass.
- **Tables:** 1 — 5-row situation-vs-algorithm decision flowchart (all-edges-non-negative-sparse → Dijkstra+binary-heap; negative-edges-no-negative-cycles → Bellman-Ford; need-to-detect-negative-cycles → Bellman-Ford; all-pairs-shortest-paths → Floyd-Warshall §10.13; unweighted graph → BFS).
- **TikZ figures:** 0
- **Topics:** "Bellman-Ford in one sentence" opening `ideabox` (relax every edge $V-1$ times; if any edge can still be relaxed → negative cycle; slower than Dijkstra but handles negative weights); full Bellman-Ford code with edge-list representation; "why $V-1$ iterations?" subsection (shortest path has at most $V-1$ edges, each iteration propagates correct distances one edge farther; $V$-th iteration relaxation = negative cycle); cost $O(VE)$ "$V$ times slower than Dijkstra worst-case, but: handles negative weights, detects negative cycles, works on any graph representation, easy to parallelize"; decision-flowchart `tabular` (5-row); SPFA `notebox` ("Shortest Path Faster Algorithm" — queue-based refinement, only re-process vertices whose distance changed; worst case still $O(VE)$, average behavior much better on real graphs).
- **Cross-references:** §10.13 (Floyd-Warshall via decision flowchart row); §10.9 (Dijkstra via decision flowchart).
- **Depth markers:** $V-1$ iteration count derived from path-edge bound. SPFA name-dropped with one-line refinement description. "Easy to parallelize" claim is real-systems content.

### 10.11 Topological Sort — lines 587–664
- **Subsections:** Kahn's algorithm (in-degree method) · DFS-based topological sort · Cost · Kahn vs.\ DFS
- **Callouts:** D×1, I×1, W×0, E×0, N×0
- **Code listings:** 2 — (1) `topoSortKahn` building in-degree array, queueing zero-in-degree vertices, peeling, returning empty on cycle detection; (2) `dfsTopo` post-order push + `topoSortDfs` driver with reversal.
- **Tables:** 0
- **TikZ figures:** 0
- **Topics:** Topological-sort formal definition (linear ordering of DAG vertices such that every edge $u \to v$ has $u$ before $v$; exists iff acyclic; not unique); canonical-use-case `ideabox` ("do A before B" constraints; course prereqs, build orders, spreadsheet evaluation, task scheduling, dataflow execution — "all the same problem"); Kahn's algorithm code with automatic cycle detection ("if final order doesn't have all $n$ vertices, the remaining form a cycle"); DFS-based code with post-order push + reverse rationale ("DFS finishes deeper-in-the-dependency-chain vertices first; in topological ordering those should come last; reversing puts them in the right order"); cost $O(V+E)$ for both; Kahn-vs-DFS bullets ("Kahn iterative + natural for streaming/parallel — multiple ready-vertices simultaneously; DFS shorter to write + fits naturally with other DFS algorithms (SCC, cycle detection)"); "most textbooks teach DFS; most real-world schedulers (Tarjan's, `make`'s job dispatcher) use Kahn-style because they work on what's-ready-now incrementally."
- **Cross-references:** None to specific prior sections; Tarjan's name-dropped without algorithm body or section reference; SCC mentioned without a section.
- **Depth markers:** Cycle-detection-as-side-effect of Kahn's. Practitioner contrast (textbook teaches DFS, real schedulers use Kahn). Tarjan's algorithm name-dropped (no body or pointer — *neither here nor anywhere else in the chapter is Tarjan's SCC actually presented*).

### 10.12 Minimum Spanning Tree — lines 666–754
- **Subsections:** Kruskal's algorithm (greedy edges) · Cost · Why the greedy works · Prim's algorithm (greedy vertex frontier)
- **Callouts:** D×1, I×1, W×0, E×1, N×1
- **Code listings:** 1 — `struct DSU` with `parent` + `rank_` arrays, `find` with path compression (iterative two-line halving), `unite` with union-by-rank; `struct Edge { int u, v, w; }`; `kruskal` driver sorting edges by weight, iterating with DSU.unite gating, breaking at $n-1$ MST edges.
- **Tables:** 1 — 5-row Kruskal-vs-Prim comparison (approach / needs / cost / good when).
- **TikZ figures:** 0
- **Topics:** Spanning-tree + MST formal definitions (subset of edges connecting all vertices, $V-1$ edges, no cycles; minimum-weight version; requires connected, undirected, weighted graph); "physical intuition" `examplebox` (electric company, edges = potential power lines with cost, must connect every city, what's the cheapest wiring plan? — that's an MST); Kruskal's algorithm prose (sort by weight, add unless it would create a cycle, stop at $V-1$ edges; "would it create a cycle?" → union-find); full DSU code with path compression + union by rank; Kruskal cost $O(E \log E) = O(E \log V)$, dominated by sort, "union-find operations are near-$O(1)$ amortized"; cut-property `ideabox` (for any cut, min-weight edge crossing the cut is in some MST; Kruskal's adds next-smallest edge connecting different components → exactly a min edge across that cut → safe); Prim's algorithm one-paragraph sketch (start any vertex, repeatedly add cheapest edge connecting "inside" to "outside"; structurally Dijkstra with different relaxation rule — "compare edge weight directly, not cumulative distance"); Kruskal-vs-Prim `tabular`; closing `notebox` (dense graph $E = \Theta(V^2)$ → Prim with array-PQ runs $O(V^2)$, beats Kruskal's $O(V^2 \log V)$; sparse → essentially tied).
- **Cross-references:** None to specific prior sections; Prim ↔ Dijkstra structural parallel called out.
- **Depth markers:** Path-compression + union-by-rank both implemented inside the listing (not just named). Cut property stated and used to argue Kruskal correctness symbolically. Prim is one paragraph + tabular + notebox — substantially less depth than Kruskal.

### 10.13 All-Pairs Shortest Path (Floyd-Warshall) — lines 756–840
- **Subsections:** Algorithm · Why it works -- the $k$-loop invariant · Cost · Floyd-Warshall vs.\ running Dijkstra $V$ times · Path reconstruction · Algebraic view
- **Callouts:** D×0, I×3, W×1, E×0, N×0
- **Code listings:** 2 — (1) three-nested-loop `floydWarshall` body operating in-place on `dist[i][j]`; (2) `next[i][j]` augmentation for path reconstruction with init rules + update rule + reconstruction-while-loop snippet.
- **Tables:** 1 — 5-row Floyd-Warshall-vs-$V$-Dijkstras comparison (total cost / handles negatives / detects negative cycles / code complexity / ideal on).
- **TikZ figures:** 0
- **Topics:** "Floyd-Warshall in one sentence" opening `ideabox` (compute shortest path from every vertex to every other; allow each vertex as intermediate in turn; three nested loops, no priority queue, $O(V^3)$); init rules (`dist[i][i] = 0`, `dist[i][j] = w(i,j)` if edge exists, $\infty$ otherwise); for-each-intermediate-$k$, for-each-pair-$(i,j)$, "does going through $k$ beat current best?" relaxation; full three-loop code with $\infty$ guards; $k$-loop-outermost-invariant `ideabox` (after $k$-th outer iteration, `dist[i][j]` is shortest using only $\{0, 1, \ldots, k\}$ as intermediates; when $k = n-1$, that's any-vertex; "$k$ loop must be outermost — swapping breaks invariant; favorite homework trap"); cost $O(V^3)$ time, $O(V^2)$ space, "tiny constants — three nested for loops of integer additions; vectorizable; absolutely flies for $V$ up to a few thousand"; FW-vs-$V$-Dijkstras tabular; negative-cycle-via-diagonal warnbox (`dist[i][i] < 0` after FW → negative cycle through $i$ — "slick, no extra pass"); path-reconstruction `next[i][j]` augmentation code; algebraic-view subsection ("Floyd-Warshall is min-plus matrix multiplication in disguise; replace 'product + sum' with 'sum + min'; adjacency matrix raised to high enough power in this semiring is exactly the all-pairs-shortest-path matrix; connects shortest paths to tropical algebra used in compiler optimization and network calculus"); chapter-closing `ideabox` (line 838, "Chapter takeaway. Graphs are the universal data structure of computer science. Every major algorithm in this chapter — BFS, DFS, Dijkstra, Bellman-Ford, topological sort, Kruskal, Floyd-Warshall — is an instance of 'explore the graph by expanding a frontier in some specific order.' The frontier type (queue, stack, priority queue, edge-sorted list, triple loop) and the relaxation rule (visit once, update-if-shorter, etc.) are what distinguish them. Internalize the pattern and you can reason about a new graph algorithm in minutes.").
- **Cross-references:** None to prior sections (algebraic view connects forward/sideways to tropical algebra, compiler optimization, network calculus — all out-of-course pointers).
- **Depth markers:** $k$-loop-outermost invariant argued symbolically. Negative-cycle-via-diagonal trick is real implementation depth. Algebraic / tropical-semiring view is genuinely advanced — out-of-textbook material for an undergraduate DSA course. Chapter-closing `ideabox` is explicitly labeled "Chapter takeaway" and unifies all 7 named algorithms under "frontier expansion + relaxation rule." Note: Prim is named in the chapter (§10.12) but **not** named in this final unification list — observed asymmetry, surfaced as inventory only.

---

## Closing material (after §10.13)

**No closing summary section.** The final `ideabox` of §10.13 (line 838, labeled "Chapter takeaway") functions explicitly as a chapter-level closer — it unifies BFS / DFS / Dijkstra / Bellman-Ford / topological sort / Kruskal / Floyd-Warshall under "frontier expansion + relaxation rule" and is the only `ideabox` in the chapter explicitly tagged as a chapter-level summary. Prim is not in the unification list (asymmetry, see §10.13 inventory above). No "what this connects to / next chapter" forward-ref box. No companion-materials line. `\end{document}` follows on line 842.

---

## `notes.tex` (compact reference, 155 lines, 2 pp)

**Layout:** Two-column `multicols`. Title: "Ch.~10 Notes — Graphs". Single-page reference, no chapter map, no narrative — definitions, case tables, gotchas.

**Subsections:** Vocabulary · Representations · BFS (unweighted shortest path) · DFS · BFS vs.\ DFS · Dijkstra (non-negative weights) · Bellman-Ford (negative weights OK) · Topological sort (DAG only) · Minimum spanning tree · Floyd-Warshall (all-pairs) · Algorithm picker · Top gotchas

**Tables:** 3 — (1) 4-row representation comparison (space / has-edge / iterate neighbors / best for); (2) 3-row BFS-vs-DFS chooser (shortest in # edges; level-by-level; small branching vs.\ topo/SCC/cycles, all paths/backtracking, deep graphs); (3) 8-row algorithm picker (reachability, unweighted shortest, $w \ge 0$ shortest, any-$w$ shortest, all-pairs, schedule DAG, MST, strong connectivity).

**Code listings:** 5 — (1) `AdjList` + `W` weighted typedef; (2) BFS body; (3) Dijkstra body with stale-skip; (4) Floyd-Warshall three-loop; (no DFS / Bellman-Ford / topo / Kruskal-Prim listings, those are prose only).

**Top gotchas (5 listed):**
1. Running Dijkstra on a graph with negative edges — silently wrong, not an error.
2. Forgetting the lazy-delete check in Dijkstra's PQ (`if (d > dist[u]) continue;`) — blows up runtime.
3. Topo sort on a graph with cycles — Kahn emits fewer than $n$ nodes; handle that case.
4. Using an adjacency matrix on a sparse graph — $O(n^2)$ space for nothing.
5. Writing BFS with a stack, or DFS with a queue. Check data structure before coding.

**Coverage relative to `lectures.tex`:**
- **Covered:** §10.1 vocabulary (compact form); §10.3–§10.4 representation comparison; §10.5 BFS code + cost; §10.6 DFS one-paragraph + BFS-vs-DFS chooser; §10.9 Dijkstra code + lazy-delete; §10.10 Bellman-Ford one-paragraph + cost; §10.11 Kahn 3-step + DFS-version one-line; §10.12 Kruskal + Prim one-paragraph each; §10.13 Floyd-Warshall code + cost; algorithm picker covering all 7 algorithms (Tarjan/Kosaraju named for SCC); 5 gotchas.
- **Not covered (intentionally — quick-reference card scope):** §10.1 dense-vs-sparse Facebook concrete example; §10.2 application enumeration (compiler/spreadsheet/package/build/state-machine/game-AI/neural-net); §10.3–§10.4 OOP `struct Vertex` form, $V^2$ catastrophe warning with concrete byte counts, edge-list third-representation; §10.5 mark-on-enqueue invariant with rationale, FIFO-preserves-shortest-path proof, six application bullets; §10.6 iterative-DFS-mark-on-pop subtlety, recursion-depth gotcha, ordering-vs-layering framing; §10.7 cycle-as-contradiction framing, kidney-exchange example, in/out-degree / source / sink terminology; §10.8 weighted-vs-unweighted shortest counterexample, Johnson's-algorithm name; §10.9 greedy-correctness symbolic proof, PQ-implementation cost table with Fibonacci heap, path reconstruction code; §10.10 $V-1$-iteration derivation, decision flowchart, SPFA; §10.11 Kahn-vs-DFS practitioner contrast (textbooks vs.\ schedulers), DFS post-order rationale; §10.12 union-find code with path compression + union by rank, cut-property correctness, dense-graph Prim-with-array-PQ note; §10.13 $k$-loop-outermost invariant, negative-cycle via diagonal, path-reconstruction `next[]` augmentation, algebraic / tropical-algebra view, "Chapter takeaway" closer.

**Density:** Quick-reference card — heavier on tables (3 tabulars across 12 subsections) than ch_9's notes (2 tabulars across ~12 subsections). Algorithm-picker table is the most useful "what do I reach for" surface; the lectures.tex chapter has no equivalent single chooser (it scatters mini-chooser tables across §10.6 BFS-vs-DFS, §10.10 Dijkstra-vs-Bellman-Ford, §10.12 Kruskal-vs-Prim, §10.13 FW-vs-$V$-Dijkstras). DFS / Bellman-Ford / topo / MST listings absent from notes — code-density is concentrated on BFS + Dijkstra + Floyd-Warshall.

---

## Cross-references inside ch_10

- §10.4 → §10.12 (edge-list `notebox` mentions Kruskal without section number).
- §10.4 → §10.13 (matrix-power-walk-count claim previews the algebraic-view subsection).
- §10.6 → §10.9, §10.11 (closing `ideabox` previews "Dijkstra and shortest paths from BFS-style frontier expansion" + "topological sort and SCC from DFS finish times").
- §10.7 → none-internal (cycle-detection-via-DFS-recursion-flag mentioned but not coded; SCC / Tarjan never get a section).
- §10.8 → §10.9 (Dijkstra forward), §10.10 (Bellman-Ford forward, with Johnson's algorithm name-dropped).
- §10.10 → §10.13 (decision flowchart row), §10.9 (decision flowchart row).
- §10.12 → §10.9 (Prim ↔ Dijkstra structural parallel).
- §10.13 → none-internal in body, but the closing chapter-takeaway `ideabox` enumerates BFS / DFS / Dijkstra / Bellman-Ford / topo / Kruskal / Floyd-Warshall (omits Prim — see inventory note in §10.13).

## Forward- / backward-references to other chapters

- **Pre-§10.1 opening `ideabox` (line 12):** Generic refs to linked list / tree / heap / hash-chained bucket as graph specializations. No section numbers; not pointers to specific prior chapter material, just thematic framing.
- **No backward refs to specific prior chapter sections by number.** Graphs are introduced fresh, not as an extension of a ch_N construct.
- **No forward refs to ch_11+.** ch_10 is a leaf in the chapter dependency graph as it currently sits — the chapter-takeaway `ideabox` closes thematically without "next, hash tables / network flow / ...".

---

## Stylistic patterns observed

1. **Survey register, not derivation register.** Unlike ch_9 (Fibonacci recurrence proof, named-case rebalancing taxonomies, CLRS-divergence callout) or ch_7 (heap operations derived from scratch), ch_10 takes 13 sections to cover graphs broadly. Each algorithm gets a one-paragraph "in one sentence" `ideabox`, idiomatic C++ snippet, cost note, and short rationale. Most rationale is one-paragraph prose, not multi-step proof.
2. **High `ideabox` density.** 15 ideaboxes — the most of any chapter so far (ch_9 had 11). Pattern: open each algorithmic section with an "X in one sentence" `ideabox`, then close with a "why it works" or chooser `ideabox`. §10.13 alone has 3 ideaboxes (opening one-sentence, $k$-loop invariant, chapter-takeaway closer).
3. **Zero TikZ figures.** Notable. ch_9 had 29 (heavy tree diagrams); ch_7 had 8; ch_10 has none. Graph algorithms get no visual frontier-expansion / before-after figures; the chapter relies entirely on prose + code + small chooser tables. This is the largest stylistic gap from ch_7 and ch_9.
4. **Heavy `tabular` use for choosers and cost analyses.** 6 tabulars across 13 sections — adjacency-list-vs-matrix (§10.4), BFS-vs-DFS (§10.6), Dijkstra-runtime-by-PQ (§10.9), Dijkstra-vs-Bellman-Ford-flowchart (§10.10), Kruskal-vs-Prim (§10.12), Floyd-Warshall-vs-$V$-Dijkstras (§10.13). Notes.tex adds a master "algorithm picker" tabular that lectures.tex does not have.
5. **Idiomatic C++ register, not templated-DS register.** ch_10 uses `std::vector<std::vector<int>>`, `std::queue<int>`, `std::priority_queue<...>` with concrete `int` vertex-IDs throughout — no `template <typename T>` `Graph<T>` abstraction. Compare ch_9's templated `AVLNode<T>` / `RBNode<T>`. ch_10 reads more like competitive-programming / interview style (the §10.9 stale-entry warnbox explicitly says "what you'd write in interviews or contests").
6. **Production / industry citations sparse but present.** NetworkX, boost::graph, JDK graph APIs (§10.3); `make`, `bazel`, `cmake` (§10.2); Tarjan's, `make`'s job dispatcher (§10.11). Less practitioner-density than ch_9's `std::map` / Linux `rbtree.c` / `std::_Rb_tree_node` register.
7. **Three named-but-not-implemented algorithms.** A* (§10.2 game AI bullet — "industry standard" but no body), Johnson's algorithm (§10.8 negative-weights warnbox — name-only), Tarjan's / Kosaraju's SCC (§10.11 Kahn-vs-DFS bullet + notes.tex algorithm picker — name-only, no body). Cycle-detection-on-directed-graphs is described as "DFS plus a 'currently in recursion' flag" (§10.7) but not coded. Surfaced as inventory observation, not finding.
8. **Insert/remove operations not present** (trivially, since graphs in this chapter are static). No `addVertex` / `removeVertex` / `removeEdge` cost analysis beyond the one-line per-representation bullets in §10.3 and §10.4.
9. **No chapter-opening structured map.** Pre-§10.1 `ideabox` is a one-paragraph thematic frame ("Why graphs matter") rather than the structured "Where this sits / toolkit additions / mastery checklist" `ideabox` of ch_1–ch_6. Matches ch_7 / ch_9.
10. **No mastery checklist.** Absent (matches ch_7 / ch_9). Step 2 will need to decide whether one belongs.
11. **Explicit chapter-takeaway closer** (line 838). Unlike ch_7 / ch_9, ch_10 has an `ideabox` *labeled* "Chapter takeaway" that explicitly unifies the chapter's algorithms under one mental model (frontier expansion + relaxation rule). It functions as a real chapter-level summary, not just a §-level closer that happens to be last.

---

## Counts at a glance

| Metric | Count |
|---|---|
| Sections (numbered 10.1–10.13) | 13 |
| Subsections (`\subsection*`, unnumbered) | 47 |
| `defnbox` (definitions) | 7 |
| `ideabox` (key ideas) | 15 |
| `warnbox` (gotchas) | 7 |
| `examplebox` (worked examples) | 3 |
| `notebox` (asides) | 5 |
| **Total callout boxes** | **37** |
| `lstlisting` (code blocks) | 16 |
| `tabular` (tables) | 6 |
| `tikzpicture` (figures) | 0 |
| Mastery checklist items | 0 (no checklist) |
| Lines (`lectures.tex`) | 842 |
| Pages (`lectures.pdf`) | 18 |
| Lines (`notes.tex` compact) | 155 |
| Pages (`notes.pdf`) | 2 |
