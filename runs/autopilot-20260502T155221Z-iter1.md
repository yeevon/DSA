# Roadmap selection — 2026-05-02
**Verdict:** NEEDS-CLEAN-TASKS
**Decision rule:** sequential (Filter 1a fired)
**Recommendation target:** `design_docs/milestones/m5_phase5_review_loop/`

## Reasoning

M1 through M4 are all closed. M4 (`✅ done 2026-05-02`, T01–T08) fully satisfies its exit criteria per the milestone README. The sequential walk moves to M5 (`Phase 5 — Review loop`) as the next open milestone.

M5's milestone directory contains only `README.md` — there is no `tasks/` subdirectory and no individual `T<NN>_<slug>.md` spec files. Filter 1a applies immediately: no specs exist for this milestone. The walk stops here; the orchestrator must run `/clean-tasks design_docs/milestones/m5_phase5_review_loop/` to generate and harden the per-task specs before any implementation work can proceed.

## Sequential walk

| Milestone | Has specs? | Hardened? | Trigger? | Deps? | Outcome |
|---|---|---|---|---|---|
| M1 | Y | Y | n/a | n/a | closed ✅ — skip |
| M2 | Y | Y | n/a | n/a | closed ✅ — skip |
| M3 | Y | Y | n/a | n/a | closed ✅ — skip |
| M4 | Y | Y | n/a | n/a | closed ✅ — skip |
| M5 | N | n/a | n/a | n/a | **NEEDS-CLEAN-TASKS** — Filter 1a: no `tasks/` directory, no `T<NN>_<slug>.md` specs exist |

Walk stopped at M5 per Filter 1 rule.

## Bug-blocker override (if applied)

Not applicable — walk did not reach a Phase-2 candidate; no override check triggered.

## Decisions the orchestrator should expect

- M5's README carries one HIGH carry-over from M4 (`M4-T05-SEC-HIGH — stripSolution() helper required before GET /api/review/due`). `/clean-tasks` must incorporate this as an explicit AC on the T02 spec (GET /api/review/due), as already described in the `## Carry-over from prior audits` section of the M5 README.
- M5 has an open architecture decision to resolve: FSRS vs SM-2 (architecture.md §5 row 3). The README leans FSRS via `ts-fsrs`; the clean-tasks pass should confirm this is spec-level-resolved so the T01 spec can reference it without ambiguity.

## Memory entries consulted

- `project_chapter_review_progress.md` — SNHU-required arc complete (ch_1–ch_6); optional chapters deferred. No impact on M5 selection.
- `feedback_aiw_uvx_oneshot.md` — uvx one-shot test pattern applies to any M5 workflow smoke (not yet relevant).
- `project_coding_practice_purpose.md` — `coding_practice/` is prompt corpus; M5 does not touch it.
- CHANGELOG 2026-05-02 most-recent section confirms M4 T08 is the last landed work; no in-flight M5 work overlaps.
