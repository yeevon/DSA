# Queue selection — 2026-05-02

## Selected task
- ID: `M5-T01`
- Spec: `design_docs/milestones/m5_phase5_review_loop/tasks/T01_fsrs_integration.md`

## Why this task
- Phase-N-blocking? Yes. T01 is the head of M5's two dependency chains (T01 → T02 → T03; T04 → T05 → T06). Without T01, T02's `GET /api/review/due` cannot read an up-to-date `fsrs_state`, and T03's `/review` page has nothing meaningful to schedule. All five remaining M5 tasks either directly depend on T01 or depend on tasks that do.
- Milestone alignment: M5 is the active next milestone (M4 closed 2026-05-02). T01 is the lowest-numbered, highest-leverage task in the milestone.
- Dependency graph state: M3 closed (T2 `fsrs_state` schema done 2026-04-24, T3 API routes done 2026-04-24). M4 closed (T06 `POST /api/attempts` done 2026-05-02, T08 `PATCH /api/attempts/[id]/outcome` done 2026-05-02). All upstream tasks T01 cites are `done`.

## Dependencies checked
- M3 `fsrs_state` table + PATCH route: `T2_drizzle_schema.md` status ✅ done 2026-04-24; `T3_api_routes.md` status ✅ done 2026-04-24.
- M4 T06 `POST /api/attempts` mc/short eval: ✅ done 2026-05-02.
- M4 T08 `PATCH /api/attempts/[id]/outcome`: ✅ done 2026-05-02.
- Carry-over from prior audits: 0 (T01's `## Carry-over from prior audits` section is empty per spec).

## Blockers checked
- Issue file status: task_analysis.md (the M5 batch analysis file) verdict `LOW-ONLY` for T01 — all HIGH/MEDIUM resolved in round 2. No per-task `T01_issue.md` exists yet (not yet audited — that is normal at `todo` state). PASS.
- Architecture decisions required: none. H-1 (server-side FSRS drift) was resolved by the architecture.md §3.5 amendment committed as `0c28a71` in iter 1.

## Risks
- Verification surface: named smoke `node scripts/fsrs-smoke.mjs` is required (new harness, Builder must create it). Spec AC-4 defines the exact assertions and expected footer. Auditor must run it.
- Dependency-manifest impact: T01 adds `ts-fsrs` to `package.json` + `package-lock.json` — dep-audit gate is mandatory per CLAUDE.md and is called out explicitly in T01's verification plan.
- Long-running likelihood: `**Long-running:** no` in spec. Single focused session covers the wrapper, the attempts handler patch, the questions/bulk seeding, the smoke, and the CHANGELOG. Low risk of multi-cycle spill.

## Recommended command
- `/clean-implement M5-T01`
